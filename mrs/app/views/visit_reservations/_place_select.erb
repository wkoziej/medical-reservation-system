<div id="place_select">
<%= collection_select(:place, :id, places, :id, :name, 
    options = {:prompt => t(:choose_place)}, 
{ :onChange => remote_function( :url => {:action => "place_changed"}, :with => "'place_id='+this.value")} ) 
%>
</div>
